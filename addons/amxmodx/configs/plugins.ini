; AMX Mod X plugins

; Admin Base - Always one has to be activated
admin.amxx		; admin base (required for any admin-related)
;admin_sql.amxx		; admin base - SQL version (comment admin.amxx)

; Basic
admincmd.amxx		; basic admin console commands
adminhelp.amxx		; help command for admin console commands
adminslots.amxx		; slot reservation
multilingual.amxx	; Multi-Lingual management

; Menus
menufront.amxx		; front-end for admin menus
cmdmenu.amxx		; command menu (speech, settings)
plmenu.amxx		; players menu (kick, ban, client cmds.)
;telemenu.amxx		; teleport menu (Fun Module required!)
mapsmenu.amxx		; maps menu (vote, changelevel)
pluginmenu.amxx		; Menus for commands/cvars organized by plugin

; Chat / Messages
adminchat.amxx		; console chat commands
antiflood.amxx		; prevent clients from chat-flooding the server
; Both off deliberately - they only ever showed the stock "Welcome to
; %hostname% / This server is using AMX Mod X" advertising (amxx.cfg:38-45) and
; both cost HUD channels, of which the client only has four. scrollmsg held
; channel 2 permanently and redrew rapidly to animate its scroll; imessage used
; an auto-channel every 180 seconds. Freeing them gives every other plugin a
; better chance of keeping the message it drew - see zp_damage_combo's header on
; why that matters here.
;scrollmsg.amxx		; displays a scrolling message
;imessage.amxx		; displays information messages
adminvote.amxx		; vote commands

; Map related
nextmap.amxx		; displays next map in mapcycle
mapchooser.amxx		; allows to vote for next map
timeleft.amxx		; displays time left on map

; Configuration
pausecfg.amxx		; allows to pause and unpause some plugins
statscfg.amxx		; allows to manage stats plugins via menu and commands

; Counter-Strike
;restmenu.amxx		; restrict weapons menu
; Off deliberately. It drew four panels - killer info, Attackers, Victims and
; the end-round summary - and the first three take an auto HUD channel each and
; hold it for amx_statsx_duration, 12 seconds (amxx.cfg:210). The client only
; has four channels. Since this server respawns you about a second after you
; die, you are back to fighting while statsx still owns three of them, which is
; why zp_damage_combo's payout announcement went missing in bursts after a
; death. See that plugin's header.
;statsx.amxx		; stats on death or round end (CSX Module required!)
;miscstats.amxx		; bunch of events announcement for Counter-Strike
;stats_logging.amxx	; weapons stats logging (CSX Module required!)


; Custom - Add 3rd party plugins here
